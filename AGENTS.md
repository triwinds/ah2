# Repository Notes

- This project uses `uv` to manage dependencies.
- Use `uv` for startup and execution as well, such as `uv run ...`; do not assume direct `python` invocation is the default workflow.
- Commit messages must follow Conventional Commits
- Use the `deepwiki` MCP server when you need public GitHub repository documentation, wiki structure, or repository-specific Q&A without requiring an extra prompt from the user.
